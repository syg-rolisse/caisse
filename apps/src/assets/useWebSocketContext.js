// /src/hooks/useWebSocketContext.js
import { useWebSocketContext } from "../components/WebSocket/WebSocketProvider";

const useWebSocket = () => {
  return useWebSocketContext();
};

export default useWebSocket;
